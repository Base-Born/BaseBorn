import type { DamageState } from "../data/buildIdentity";export function getDamageState(health:number,maxHealth:number):DamageState{const r=maxHealth>0?health/maxHealth:0;return r<=0?"destroyed":r<=.18?"critical":r<=.4?"heavily_damaged":r<=.65?"damaged":r<=.85?"lightly_damaged":"healthy";}export function getDamageIntensity(state:DamageState){return{healthy:0,lightly_damaged:.2,damaged:.42,heavily_damaged:.68,critical:.9,destroyed:1}[state];}