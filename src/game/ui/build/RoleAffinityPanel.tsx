import type { BuildIdentitySnapshot } from "../../data/buildIdentity";export function RoleAffinityPanel({build}:{build:BuildIdentitySnapshot}){const roles=Object.entries(build.roleAffinities).sort((a,b)=>b[1]-a[1]).slice(0,4);const max=Math.max(1,...roles.map(([,v])=>v));return <section className="identityRoles"><header><span>ROLE IDENTITY</span><strong>{build.primaryRole}<i>/ {build.secondaryRole}</i></strong></header>{roles.map(([role,value])=><div key={role}><span>{role}</span><i><b style={{width:value/max*100+"%"}}/></i><em>{Math.round(value)}</em></div>)}</section>}