import type { BuildIdentitySnapshot } from "../../data/buildIdentity";import { RoleAffinityPanel } from "./RoleAffinityPanel";import { PowerMassHeatPanel } from "./PowerMassHeatPanel";import { ModuleSynergyPanel } from "./ModuleSynergyPanel";import { StrengthWeaknessPanel } from "./StrengthWeaknessPanel";export function BuildIdentityPanel({build}:{build:BuildIdentitySnapshot}){return <section className="buildIdentityPanel"><header><div><span>EMERGENT BUILD</span><strong>{build.roleLabel}</strong><small>{build.structuralBranch.name} · {build.teamContribution}</small></div><b className={"damageState damageState--"+build.damageState}>{build.damageState.replace(/_/g," ")}</b></header><RoleAffinityPanel build={build}/><PowerMassHeatPanel build={build}/><StrengthWeaknessPanel build={build}/><ModuleSynergyPanel build={build}/>{build.installedModules.length>0&&<div className="identityModules">{build.installedModules.map(m=><span className={m.enabled?"":"disabled"} key={m.slotId}><b>{m.name}</b><small>{m.family} · {m.visual}</small></span>)}</div>}</section>}