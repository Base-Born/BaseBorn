import type { BuildIdentitySnapshot } from "../../data/buildIdentity";export function ModuleSynergyPanel({build}:{build:BuildIdentitySnapshot}){return <section className="identitySynergies"><header><span>SYNERGIES</span><b>{build.activeSynergies.length} ACTIVE</b></header>{build.activeSynergies.map(s=><article key={s.id}><strong>{s.name}</strong><span>{s.effect}</span><small>{s.visualEffect}</small></article>)}{!build.activeSynergies.length&&build.availableSynergies.slice(0,1).map(s=><article className="locked" key={s.id}><strong>{s.name}</strong><span>Missing: {s.missing.join(", ")}</span></article>)}</section>}