import { Sparkles } from "lucide-react";import type { GameSnapshot } from "../../types";export function UpgradeCompletionOverlay({feedback}:{feedback:GameSnapshot["upgradeFeedback"]}){if(!feedback)return null;return <aside className="upgradeCompletion" role="status"><Sparkles size={20}/><div><span>UPGRADE COMPLETE</span><strong>{feedback.title}</strong><p>{feedback.before} <b>→</b> {feedback.after}</p><small>{feedback.visualChange}</small><em>Trade-off: {feedback.drawback}</em></div></aside>}